name: NFL Weekly Odds

on:
  schedule:
    - cron: '0 7 * * 3'
  workflow_dispatch:

jobs:
  nfl-odds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node nfl-odds-weekly.js
